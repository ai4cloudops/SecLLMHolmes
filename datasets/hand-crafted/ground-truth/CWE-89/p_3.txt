The user provided `username` and `password` are passed to `execute` function following the pymysql parametrized query syntax for using user defined inputs. Even if a user adds a malicious query to the `user_input`, it will be sanitized, and will prevent an SQL injection attack.