The user provided `username` and `password` are concatenated to the SQL statement in `createQuery` and passed to `execute` function without following the pymysql parametrized query syntax for using user defined inputs. A user can add malicious query to the `user_input`, leading to an SQL injection attack.