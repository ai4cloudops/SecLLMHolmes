The user input `title` is reflected back and rendered on the web page after proper escaping. Now, even if `title` contains malicious scripts, they will be rendered as part of the HTML response but will never be executed when the response is rendered by the browser.