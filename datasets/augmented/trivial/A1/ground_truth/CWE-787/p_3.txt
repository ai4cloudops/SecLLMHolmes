In the `get_hostname` function the length of the `xt` can be greater than the `MAX_NAME_LEN`, hence the program checks for it beforehand and returns NULL if it is greater. This mitigates the out of bound write vulnerability.