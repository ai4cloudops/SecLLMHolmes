The program follows the sqlalchemy parametrized query syntax to use `user_input` to insert and execute an SQL query in `insert_book` and `find_book` functions, respectively. Now even a user adds a malicious query to the `pp` or `hd`, it will be sanitized, and will prevent an SQL injection attack.